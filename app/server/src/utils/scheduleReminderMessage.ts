

// const scheduleReminderMessage = async (clientInstance: any, phoneNumber: string) => {
//   setTimeout(async () => {
//     try {
//       await clientInstance.sendText(
//         `${phoneNumber}@c.us`,
//         "📌 Enregistrez notre numéro pour voir nos produits et services dans notre statut WhatsApp."
//       );
//     } catch (error) {
//       console.error("❌ Erreur lors de l'envoi du message de rappel:", error);
//     }
//   }, 60 * 60 * 1000); // 1 heure
// };

// export default scheduleReminderMessage;